{
  "devDependencies": {
    "esbuild": "^0.25.2",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=extension-saver --format=iife --target=es2017 && chmod +x extension-saver",
    "run": "./extension-saver"
  }
}